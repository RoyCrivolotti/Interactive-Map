streetViewModule = (function () {
  var paronama; // StreetView

  function init() {
    /* TODO: creates a pano in a set position and displays it */
  }

  function setStreetView(ubicacion) {
    /* TODO: updates the pano variable position and changes the map to display the current position's StreetView */
  }

  return {
    init,
    setStreetView
  };
})();